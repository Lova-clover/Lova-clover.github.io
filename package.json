{
  "name": "han-seongju-portfolio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Static 2D portfolio with dependency-free local validation and preview.",
  "scripts": {
    "check": "node scripts/check.mjs",
    "build": "node scripts/build.mjs",
    "dev": "node scripts/serve.mjs",
    "preview": "node scripts/serve.mjs --dist"
  },
  "engines": {
    "node": ">=18"
  }
}
